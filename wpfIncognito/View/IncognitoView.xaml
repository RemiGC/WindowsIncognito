<UserControl x:Class="wpfIncognito.View.IncognitoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:wpfIncognito.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Height="100">
        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition Height="25" />
            <RowDefinition Height="25" />
        </Grid.RowDefinitions>

        <CheckBox Content="Incognito mode ON" IsChecked="{Binding Path=IsIncognitoModeOn, Mode=OneWay}" Grid.Row="0"  IsThreeState="False" IsEnabled="False" />
        <Button Content="Incognito ON" Command="{Binding IncognitoOnCommand }"  Grid.Row="1" />
        <Button Content="Incognito OFF" Command="{Binding IncognitoOffCommand }" Grid.Row="2" />
    </Grid>
</UserControl>
