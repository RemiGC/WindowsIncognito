<UserControl x:Class="wpfIncognito.View.SoftwareListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:wpfIncognito.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <ItemsControl ItemsSource="{Binding AllSoftwares}">
            <ItemsControl.ItemTemplate >
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBox Width="100" Text="{Binding Status,Mode=OneWay}" />
                        <TextBox Width="100" Text="{Binding AppIDHexa,Mode=OneWay}" />
                        <TextBox Width="100" Text="{Binding FileName,Mode=OneWay}" />
                        <TextBox Width="100" Text="{Binding AppName}" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
