<UserControl x:Class="wpfIncognito.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:wpfIncognito.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <CheckBox IsChecked="{Binding IncognitoModeOnStartup}" Content="Enable Incognito Mode on Startup"  Grid.Row="0"></CheckBox>
        <CheckBox IsChecked="{Binding MinimizeOnStartup}" Content="Start Minimized"  Grid.Row="1" ></CheckBox>
        <CheckBox IsChecked="{Binding MinimizeToSystemTray}" Content="Minimize to System Tray"  Grid.Row="2" ></CheckBox>
    </Grid>
</UserControl>
